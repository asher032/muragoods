import ModuleSettings from '../components/ModuleSettings';

export default function MusicPage() {
  return (
    <ModuleSettings
      moduleId="music"
      title="🎵 Music"
      description="Playback, DJ role, default volume and queue behaviour"
    />
  );
}
