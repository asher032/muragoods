discord.py>=2.4,<3.0
# Voice support (DAVEY protocol) was split out of discord.py — without this
# package every voice connect raises "RuntimeError: davey library needed in
# order to use voice" and no track can ever play.
davey>=0.1.0
PyNaCl>=1.5,<2.0
aiohttp>=3.9,<4.0
# The `default` extra pulls in yt-dlp-ejs, the JS challenge solver scripts.
# Their version MUST match what this yt-dlp release requires, so pip resolves
# it from yt-dlp's own metadata instead of us pinning a range that can drift
# out of sync (an ignored EJS package silently disables challenge solving).
# Note the scripts are only half the requirement — a JavaScript runtime must
# exist too; see scripts/install_deno.py.
yt-dlp[default]>=2025.5.0
# Required for yt-dlp to use HTTPS proxies (YOUTUBE_PROXY) with impersonation.
# Without it, every proxied request fails with "one of the following
# dependencies needs to be installed: requests, curl_cffi".
curl-cffi>=0.5
imageio-ffmpeg>=0.5
motor>=3.4,<4.0
python-dotenv>=1.0,<2.0
