discord.py>=2.4,<3.0
# Voice support (DAVEY protocol) was split out of discord.py — without this
# package every voice connect raises "RuntimeError: davey library needed in
# order to use voice" and no track can ever play.
davey>=0.1.0
PyNaCl>=1.5,<2.0
aiohttp>=3.9,<4.0
yt-dlp>=2025.5.0
# Official companion for yt-dlp's JS challenge solver. Pure-python (~50KB).
# Without it, signature/n-parameter solving is unavailable and extraction can
# stall or report formats as unavailable.
yt-dlp-ejs>=0.8
imageio-ffmpeg>=0.5
motor>=3.4,<4.0
python-dotenv>=1.0,<2.0
